/**
 * Centralized AI Model Configuration
 *
 * This app uses Ollama only. Model selection is handled entirely by Ollama externally.
 *
 * Environment Variables:
 * - OLLAMA_SERVICE_URL: Ollama server URL (default: http://localhost:11434)
 */

export const DEFAULT_MODEL = "ollama";